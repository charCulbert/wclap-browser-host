# Example WCLAPs

This is a collection of WCLAPs usable for testing or demos, grouped into directories depending on their source.

Each collection has its own `LICENSE.txt` (or equivalent), and optionally a README describing its source, features and compilation settings.
