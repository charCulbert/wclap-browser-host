# WCLAP examples

- `public/` contains the char effect and synth archives used by the static host.
- `fixtures/webclap/` contains additional upstream WCLAPs for regression tests.
- `static/` is the bundled single-plugin host example.
